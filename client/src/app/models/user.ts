import { Observable } from "rxjs/Observable";

export class User {
    id: number;
    email: string;
    birthdate: Date;
    sex: string;
    ntrp: string;
    description: string;
    name: string;
}