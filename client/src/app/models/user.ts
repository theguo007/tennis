import { Observable } from "rxjs/Observable";

export class User {
    _id: number;
    email: string;
    birthdate: Date;
    sex: string;
    ntrp: string;
    description: string;
    name: string;
}